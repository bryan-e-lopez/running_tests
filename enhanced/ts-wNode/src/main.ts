// src/main.ts

export function add(a: number, b: number): number {
    return a + b;
  }
  
  // Main function to run
  export function main() {
    const result = add(5, 3);
    console.log("Running the main program"); 
    console.log(`The result is: ${result}`);
  }
  