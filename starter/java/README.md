To compile a Java program, you will need to have the JVM and JDK installed. 

* run the java compiler and pass the java file as an argument `javac Main.java` 
* run the java Class with the java utility `java Main` 
